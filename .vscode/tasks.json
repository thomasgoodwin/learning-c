{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build project",
            "type": "shell",
            "command": "cd ../ && make",
            "args": [],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "build",
        }
    ]
}